class Lesson {
    constructor(element, number, name, tipsText, timesCompleted, totalTimesNeeded) {
        this.element = element
        this.number = number
        this.name = name
        this.tipsText = tipsText
        this.timesCompleted = timesCompleted
        this.totalTimesNeeded = totalTimesNeeded
    }
}